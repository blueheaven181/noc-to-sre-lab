---
ansible_user: ansible
ansible_ssh_private_key_file: ~/.ssh/lab_rsa
ansible_python_interpreter: /usr/bin/python3

rhel_release_pin: "9.5"     # matches production; see common role
selinux_state: enforcing    # never set this to disabled - fix the policy instead
firewalld_enabled: true
